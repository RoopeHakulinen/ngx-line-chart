export interface IDataSetStyle {
  circle: {
    color: string;
    radius: number;
  };
  line: {
    color: string;
    width: number;
  };
  labels: {
    yAxis: {
      color: string;
    },
    value: {
      color: string;
    }
  };
}
